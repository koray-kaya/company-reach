# Company and person data — never committed.
# Anchored to the repository root on purpose: an unanchored `data/` also
# matched `src/company_reach/data/`, which is the package's own blocklist,
# so a fresh clone had no blocklist at all. Caught by CI's first run.
/data/

# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# OS
.DS_Store

# Local, per-developer agent setup — not part of the project
.claude/
CLAUDE.local.md

# Milestone explanations — written for the repository's owner alone, and
# they quote the hand-labelled golden set, which is real company data
docs/milestones/

# Personal: the research goal and who is writing
profile.toml
